hrs = input("Enter Hours:")
hh=float(hrs) #Input function stores information as a string so it is needed to convert it into float or integer
rate=input("Enter Rate: ")
rr=float(rate)
pp=hh*rr
print("Pay:",pp)
